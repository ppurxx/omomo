spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://18.223.156.31:3306/omomo?serverTimezone=UTC&characterEncoding=UTF-8
    username: omomo
    password: omomo1414@@
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments : true

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace;